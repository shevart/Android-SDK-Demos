<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shevart.androidcorelearn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".service.ServiceLearnActivity" />

        <service android:name=".service.service_not_sticky.NotStickyService" />
        <service android:name=".service.service_not_sticky.BinderService" />

        <activity android:name=".service.BinderServiceActivity" />
        <activity android:name=".fragments.FragmentsDemoActivity" />
        <activity android:name=".fragments.mobile_and_tablet_ui.MobileAndTabletUIFragmentsDemoActivity"></activity>
    </application>

</manifest>