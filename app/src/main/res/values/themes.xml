<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.MaterialAnimation"/>

    <style name="AppTheme.MaterialAnimation.SharedViews">
        <!-- enable animations -->
        <item name="android:windowContentTransitions">true</item>

        <!-- set default animations for sharedViews between activities -->
        <item name="android:windowSharedElementEnterTransition">@transition/change_bounds</item>
        <item name="android:windowSharedElementExitTransition">@transition/change_bounds</item>
    </style>
    <style name="AppTheme.MaterialAnimation.SharedViews.UI">
        <item name="android:windowSharedElementEnterTransition">@transition/enter_shared_ui</item>
        <item name="android:windowSharedElementExitTransition">@transition/exit_shared_ui</item>
    </style>
</resources>